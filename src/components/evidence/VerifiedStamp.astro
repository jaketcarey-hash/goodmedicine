---
/**
 * The quiet end of the evidence apparatus.
 *
 * On the record surface a reader wants the full register — confidence, stage,
 * unknowns, numbered sources. On the community surface, someone reading about
 * their own child benefit wants one thing: is this number still true? This is
 * that, and nothing more: the date it was checked, in the apparatus voice.
 */
interface Props {
  date: string;
  source?: string;
  label?: string;
}

const { date, source, label = 'Checked' } = Astro.props;
---

<span class="apparatus inline-flex items-center gap-1.5 text-verified">
  <svg
    class="w-3 h-3 shrink-0"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2.5"
    stroke-linecap="square"
    aria-hidden="true"
  >
    <path d="M20 6L9 17l-5-5" />
  </svg>
  {
    source ? (
      <>
        {label} {date} &middot;{' '}
        <a href={source} rel="noopener noreferrer" target="_blank" class="underline underline-offset-2 hover:text-ink">
          source
        </a>
      </>
    ) : (
      <>
        {label} {date}
      </>
    )
  }
</span>
