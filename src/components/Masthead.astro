---
/**
 * Desktop masthead — the site's front door from 768px up.
 *
 * The phone keeps its bottom nav; this replaces it on wider screens. Both are
 * always in the markup and CSS decides which one is visible, so there is no
 * layout shift while JavaScript works out how wide the window is.
 *
 * Nations sits first and carries its own rule underneath, because it is the
 * publication — the thing that changes daily and the reason to come back.
 */
import BrandMark from './BrandMark.astro';

interface Props {
  branch?: string;
}

const { branch = 'home' } = Astro.props;

const record = [{ id: 'nations', label: 'Nations', href: '/nations' }];

const community = [
  { id: 'money', label: 'Money', href: '/money' },
  { id: 'rights', label: 'Rights', href: '/rights' },
  { id: 'path', label: 'Path', href: '/path' },
  { id: 'self', label: 'Self', href: '/self' },
] as const;

const utility = [
  { id: 'tools', label: 'Tools', href: '/tools' },
  { id: 'glossary', label: 'Glossary', href: '/glossary' },
];

const searchActive = branch === 'search';
---

<header
  class="hidden md:block sticky top-0 z-50 bg-ground/85 backdrop-blur-lg border-b border-rule"
>
  <div class="mx-auto max-w-6xl px-6 h-16 flex items-center gap-8">
    <a
      href="/"
      class="flex items-center gap-2.5 shrink-0 group"
      aria-label="Strong Fire — home"
    >
      <BrandMark size={26} id="masthead" />
      <span class="font-display font-semibold text-lg tracking-tight text-ink">
        Strong Fire
      </span>
    </a>

    <nav class="flex items-center gap-1 ml-auto" aria-label="Main navigation">
      {
        record.map((item) => (
          <a
            href={item.href}
            aria-current={branch === item.id ? 'page' : undefined}
            class:list={[
              'relative px-3 py-2 text-sm font-semibold tracking-tight transition-colors duration-[var(--duration-normal)]',
              'after:absolute after:left-3 after:right-3 after:-bottom-px after:h-[2px]',
              'after:bg-ink after:transition-opacity after:duration-[var(--duration-normal)]',
              branch === item.id
                ? 'text-ink after:opacity-100'
                : 'text-quiet hover:text-ink after:opacity-0 hover:after:opacity-30',
            ]}
          >
            {item.label}
          </a>
        ))
      }

      <span class="w-px h-5 bg-rule mx-2" aria-hidden="true"></span>

      {
        community.map((item) => (
          <a
            href={item.href}
            aria-current={branch === item.id ? 'page' : undefined}
            class:list={[
              'relative px-3 py-2 text-sm font-medium transition-colors duration-[var(--duration-normal)]',
              'after:absolute after:left-3 after:right-3 after:-bottom-px after:h-[2px]',
              'after:bg-ink after:transition-opacity after:duration-[var(--duration-normal)]',
              branch === item.id
                ? 'text-ink after:opacity-100'
                : 'text-quiet hover:text-ink after:opacity-0 hover:after:opacity-30',
            ]}
          >
            {item.label}
          </a>
        ))
      }

      <span class="w-px h-5 bg-rule mx-2" aria-hidden="true"></span>

      {
        utility.map((item) => (
          <a
            href={item.href}
            aria-current={branch === item.id ? 'page' : undefined}
            class:list={[
              'px-3 py-2 text-sm transition-colors duration-[var(--duration-normal)]',
              branch === item.id
                ? 'text-ink font-medium'
                : 'text-faint hover:text-ink',
            ]}
          >
            {item.label}
          </a>
        ))
      }

      <a
        href="/search"
        aria-label="Search"
        aria-current={searchActive ? 'page' : undefined}
        class:list={[
          'ml-1 p-2 rounded-sm transition-colors duration-[var(--duration-normal)]',
          searchActive
            ? 'text-ink bg-ink/5'
            : 'text-faint hover:text-ink hover:bg-ink/5',
        ]}
      >
        <svg
          class="w-[18px] h-[18px]"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <circle cx="11" cy="11" r="7"></circle>
          <path d="M21 21l-4.35-4.35"></path>
        </svg>
      </a>
    </nav>
  </div>
</header>
