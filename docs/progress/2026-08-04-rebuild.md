# Strong Fire rebuild — handoff, 4 August 2026

Assume you have none of the context from the session that produced this.

## Where it stands

Live on strongfire.ca, **not indexed** (deliberately — see below). 508 pages, up from
~50. Ten commits on `rebuild-desktop-publication`, pushed, deployed to production.

The site was a phone-only PWA locked to a 512px column, with a separate Python-rendered
news section dumped into `public/` that nothing linked to. It is now one site with a
desktop shell, the brief rendered through its own layouts, and several surfaces that
did not exist.

## What was built

- **Desktop shell.** Masthead above 768px, phone app unchanged below. CSS switches; no JS.
- **Figure registry.** 21 rates/amounts in `src/data/figures/2026.json`, each with source
  and review date. The build fails on stale ones. Fixed live errors: CCB two years stale,
  GST credit replaced by CGEB and still showing $533, GIS $18 low.
- **Nations section.** `/nations` (today), `/archive`, `/ledger` (42 records, filterable),
  `/open` (166 open questions), `/bc` (all 201 BC First Nations), `/who`.
- **Search** across all 508 pages, built from rendered HTML, precached for offline.
- **Glossary** split from one page into 89.
- **Reach.** Sitemap, robots, canonical, Open Graph, share card. Every page has its own
  description.
- **`/what-applies`** — seven money situations, what decides them, what is settled, what
  is not, and which kind of professional to ask.
- **Landing page** — news down one side, directory down the other.

## Things that were wrong and are now fixed

Worth knowing because they show the failure modes:

1. The Indigenous Leadership Circle was described on the live site as an RBC employee
   network. It is independent. Same error was in personal memory.
2. Two of three "CRITICAL" items in the monthly audit were false positives. Applying them
   would have put real errors into a live tax tool.
3. A first pass attributed ledger records to 198 of 201 BC Nations — normalising "Nation"
   away left empty-string keys that match everything.
4. 33 pages shared one meta description, including the ones people search for.
5. Search ranked glossary stubs above the full guides.

Four of those five were found by **looking at the live site in a browser**, not by
reading code. That is the lesson.

## In flight / not done

- ~~**Design.**~~ **Done after this doc was written** — commit `e4c0ed0`. The two-pass run
  retired the serif and the cream and made the stage rail the signature. The reasoning is
  in `DECISIONS.md` under "The Record has no serif and no cream". Verified at 375/768/1440.

  **It does not look very different, and that is fair.** A serif removed, a background
  changed from `#fffdf8` to `#ffffff`, one small new element. Layout, spacing, hierarchy,
  component shapes and the colour system are untouched. It corrected a wrong signal — the
  site now reads as one thing rather than an app with a publication bolted alongside —
  but nobody would call it a new design. If a genuinely different look is wanted, brief
  it separately, and do it **before** more writing so the depth work does not get poured
  into a layout that is about to move.
- **Depth.** The thinnest pages are the tools: `/self/benefits` is 37 words of wrapper
  around a widget, `/calendar` 43, `/money/debt-planner` 53. "When she gets there, the
  answer is there" currently fails on those.
- **18 article pages still stamped March 2026** — never reviewed, only the figure-bearing
  pages were.
- **66 ledger candidates** in `~/jakexclaude/nations-brief/ytd-2026/ledger-candidates.json`
  waiting on stage and confidence before they can merge.
- **157 of 201 BC Nation pages** have nothing tracked. Correct, but thin.
- Name and logo are still open pre-launch items.

## Needs Jake, not Claude

- `cd ~/jakexclaude && claude` and accept the trust dialog once. 14 permission entries are
  being dropped on every scheduled run from that directory — all the briefs, not just
  Nations.
- `~/jakexclaude/nations-brief/` is untracked in that repo; the pipeline changes
  (`export_to_strongfire.py`, `promote_to_ledger.py`) are uncommitted there.
  `~/automation/` is not a repo at all, so `publish-nations-brief.sh` has no history.
- Whether to flip `INDEXABLE` when the build-out is done.

## Next three steps

*Step 1 was the design run, now done — see In flight above. The list below is what remains.*

**Before any of it: is there a redesign coming?** `docs/design-brief.md` (written 5 Aug)
works up three directions to token systems and wireframes, with a recommendation. If one
is being built, it goes **first** — otherwise the writing below gets poured into a layout
that then moves, and you pay for it twice.

1. Depth pass on the tool pages — each needs the surrounding explanation, not just the
   widget. `/self/benefits` (37 words), `/calendar` (43), `/money/debt-planner` (53).
   This is the biggest gap between the site and its own test.
2. Review the 18 March-2026 pages.
3. Stage and score the 66 ledger candidates in
   `~/jakexclaude/nations-brief/ytd-2026/ledger-candidates.json`.

Build verified clean on 5 Aug: 508 pages, figures gate passed, 510 indexed, 517 precached.

## Do not

- Rebuild this as a publication. See CLAUDE.md.
- Re-flag the audit items listed in REVIEW.md — they were verified against primary sources.
- Sum the ledger amounts.
- Loosen the Nation matching rules without reading DECISIONS.md first.
